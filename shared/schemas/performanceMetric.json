{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PerformanceMetric",
  "type": "object",
  "properties": {
    "portfolioId": {"type": "string"},
    "return": {"type": "number"},
    "benchmark": {"type": "number"},
    "timestamp": {"type": "string", "format": "date-time"}
  },
  "required": ["portfolioId", "return", "benchmark", "timestamp"]
}
